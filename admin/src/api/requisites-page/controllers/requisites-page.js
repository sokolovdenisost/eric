'use strict';

/**
 * requisites-page controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::requisites-page.requisites-page');
